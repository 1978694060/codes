set -g prefix C-a
unbind C-b
bind C-a send-prefix

unbind '"'
bind - splitw -v
unbind %
#bind | splitw -h
bind \ splitw -h

bind k selectp -U
bind j selectp -D
bind h selectp -L
bind l selectp -R

set-option -g base-index 1
set-window-option -g pane-base-index 1
